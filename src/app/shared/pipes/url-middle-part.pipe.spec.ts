import { UrlMiddlePartPipe } from './url-middle-part.pipe';

describe('UrlMiddlePartPipe', () => {
  it('create an instance', () => {
    const pipe = new UrlMiddlePartPipe();
    expect(pipe).toBeTruthy();
  });
});
